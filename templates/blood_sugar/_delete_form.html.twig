<form method="post" action="{{ path('app_blood_sugar_delete', {'id': blood_sugar.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blood_sugar.id) }}">
    <button class="btn">Delete</button>
</form>
