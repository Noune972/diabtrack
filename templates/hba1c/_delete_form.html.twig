<form method="post" action="{{ path('app_hba1c_delete', {'id': hba1c.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hba1c.id) }}">
    <button class="btn">Delete</button>
</form>
